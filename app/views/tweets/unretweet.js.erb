$("#tweet<%= @tweet.id %> a.retweetbtn").replaceWith(
  "<%= escape_javascript(render partial: 'retweet', locals: {tweet: @tweet}) %>");
$("div.tweets_count a").text("<%= current_user.tweets.size %>")
