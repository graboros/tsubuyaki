-if users.any?
  - users.each do |user|
    .follow_user.clearfixt{id: "user#{user.id}"}<
      .col-xs-4<
        .panel.panel-default<
          .panel-heading{"style"=> "background-color: #0083B4; height: 90px;"}
            = render 'follow', user: user
            - if user.profile.present? and user.profile.image.present? 
              = image_tag user.profile.image.url, class: "img-rounded userimage"
            - else
              = image_tag 'no_image1.png', class: "img-rounded userimage"
            .username<
              = link_to user.username, user_path(user)
          .panel-body
            %div{"style"=> "font-weight: bold;"}<
- else
  .no_user.list-group-item<
    該当のユーザは存在しません
